services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: measurements
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  audio-worker:
    build: ./audio-worker
    volumes:
      - ./recordings:/recordings
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/measurements
      RECORDINGS_DIR: /recordings
      POLL_INTERVAL_S: 5
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  grafana:
    image: grafana/grafana:10.4.0
    ports:
      - "3000:3000"
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
      - grafana_data:/var/lib/grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    depends_on:
      - postgres

  # --- BONUS: uncomment to enable the trigger API ---
  # api:
  #   build: ./bonus/api
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./recordings:/recordings
  #   environment:
  #     RECORDINGS_DIR: /recordings
  #   depends_on:
  #     - audio-worker

  # --- BONUS: uncomment to enable the web UI ---
  # ui:
  #   build: ./bonus/ui
  #   ports:
  #     - "5173:80"
  #   depends_on:
  #     - api

volumes:
  postgres_data:
  grafana_data:
